<LinkTo
  class='button-link control-size-{{@size}} control-variant-{{this.variant}}'
  @route={{@route}}
  @query={{this.query}}
  {{on 'click' this.handleClick}}
  ...attributes
>
  {{yield}}
</LinkTo>