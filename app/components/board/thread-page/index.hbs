<div class='thread-page'>
  <div class='thread-page-content'>
    {{#each this.posts as |post|}}
      <Board::Post
        @post={{post}}
        @avatarStyle={{this.avatarStyle}}
        @subtle={{is-post-subtle post @subtleUntilPostId @page}}
      />
    {{/each}}
  </div>
</div>