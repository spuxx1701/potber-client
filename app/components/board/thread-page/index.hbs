<div class='thread-page'>
  <div
    class='thread-page-content {{if this.makeAllPostsSubtle " post-subtle"}}'
  >
    {{#each this.posts as |post|}}
      <Board::Post @post={{post}} @avatarStyle={{this.avatarStyle}} />
    {{/each}}
  </div>
</div>