<div id='sidebar' role='navigation'>
  <Sidebar::Nav />
  <div id='sidebar-content'>
    {{#if this.authenticated}}
      <Sidebar::NewsFeed @bookmarks={{@bookmarks}} />
    {{/if}}
    <hr />
    <Sidebar::BoardFavorites @boards={{this.boardFavorites}} />
  </div>
</div>