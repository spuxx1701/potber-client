export interface FindById {
  id: string;
  name: string;
  avatarUrl?: string;
  rank?: string;
  lastLogin?: string;
  activity?: string;
  status?: string;
  age?: string;
  groupId?: string;
  locked?: boolean;
}
