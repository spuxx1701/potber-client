{{! @glint-nocheck }}{{page-title (t 'app.name')}}

{{#if @model.failure}}
  <Misc::Error::StartupFailure @error={{@model.error}} />
{{else}}
  <div id='page-content'>
    {{outlet}}
  </div>

  <Misc::Loading-Indicator />
  <Modal />

  <NotificationContainer @position='bottom' @zindex='99' />

  <Nav />

  {{#if this.authenticated}}
    <Sidebar />
  {{/if}}
{{/if}}