{{#if @model}}
  {{page-title this.pageTitle}}
  <Nav::Routes::Board @board={{@model.board}} @page={{@model.page}} />

  {{#each this.threads as |thread|}}
    <Board::Thread @thread={{thread}} />
  {{/each}}
{{else}}
  {{page-title '404'}}

  <Error::NotFound />
{{/if}}