{{page-title 'Post bearbeiten'}}

<Nav::Routes::Generic @title='Post bearbeiten' @enableBackNavigation={{true}} />

<Board::PostForm
  @post={{@model}}
  @onSubmit={{this.handleSubmit}}
  @busy={{this.busy}}
/>