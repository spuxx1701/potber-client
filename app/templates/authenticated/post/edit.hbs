{{! @glint-nocheck }}
{{page-title (t 'route.post.edit.title')}}

<Features::PostForm
  @id='edit-post-form'
  @threadOrPost={{@model.post}}
  @navTitle='{{t "route.post.edit.title"}}'
  @onSubmit={{this.handleSubmit}}
>
  <Features::PostForm::Reply @formId='edit-post-form' @post={{@model.post}} />
</Features::PostForm>