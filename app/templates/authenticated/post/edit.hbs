{{! @glint-nocheck }}{{page-title (t 'route.post.edit.title')}}

<Board::PostForm
  @post={{@model}}
  @onSubmit={{this.handleSubmit}}
  @busy={{this.busy}}
  @navTitle={{t 'route.post.edit.title'}}
/>