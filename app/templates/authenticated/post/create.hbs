{{! @glint-nocheck }}{{page-title 'Post erstellen'}}

<Nav::Routes::Generic
  @title='Post erstellen'
  @subtitle={{@model.thread.title}}
  @enableBackNavigation={{true}}
/>

<Board::PostForm
  @post={{@model.post}}
  @onSubmit={{this.handleSubmit}}
  @busy={{this.busy}}
/>

<Board::ThreadPage
  @thread={{@model.thread}}
  @reverseOrder={{true}}
  @lastReadPost='*'
/>