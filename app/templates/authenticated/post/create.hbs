{{page-title 'Post erstellen'}}

<Nav::Routes::Generic
  @title='Post erstellen'
  @subtitle={{@model.thread.title}}
/>

<Board::PostForm
  @post={{@model.post}}
  @onSubmit={{this.handleSubmit}}
  @busy={{this.busy}}
/>

<Board::ThreadPage
  @page={{@model.thread.page}}
  @reverseOrder={{true}}
  @subtleUntilPostId='*'
/>