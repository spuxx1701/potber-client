{{! @glint-nocheck }}{{page-title (t 'route.post.create.title')}}

<Board::PostForm
  @post={{@model.post}}
  @onSubmit={{this.handleSubmit}}
  @busy={{this.busy}}
  @navTitle={{t 'route.post.create.title'}}
  @navSubtitle={{@model.thread.title}}
/>

<Routes::Post::RecentPosts @thread={{@model.thread}} />