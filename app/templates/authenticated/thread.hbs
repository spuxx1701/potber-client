{{! @glint-nocheck }}{{#if @model.thread}}
  {{page-title this.pageTitle}}

  <Nav::Routes::Thread
    @thread={{@model.thread}}
    @bookmark={{@model.bookmark}}
  />

  <Board::ThreadPage
    @thread={{@model.thread}}
    @avatarStyle={{@model.avatarStyle}}
    @lastReadPost={{@model.lastReadPost}}
  />
{{else}}
  {{page-title '404'}}
  <Misc::Error::NotFound />
{{/if}}