{{#if @model.thread}}
  {{page-title this.pageTitle}}

  <MainNav::Thread
    @thread={{@model.thread}}
    @currentPage={{@model.currentPage}}
  />

  <Board::ThreadPage
    @page={{@model.thread.page}}
    @avatarStyle={{@model.avatarStyle}}
  />
{{else}}
  {{page-title '404'}}

  <Error::NotFound />
{{/if}}